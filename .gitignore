# Django
*.log
*.pot
*.pyc
__pycache__/
local_settings.py
db.sqlite3
db.sqlite3-journal

# Virtual environments
.venv/
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Python build artifacts
*.py[cod]
*$py.class
*.so
build/
dist/
*.egg-info/
*.egg

# Logs
logs/*.log

# Google Cloud / Firebase credentials (sensitive - never commit)
*gen-lang-client*.json
*service-account*.json
google-credentials.json
credentials.json
serviceAccountKey.json

# Environment files
*.env
*.env.*
!.env.example
!backend/.env.example
!frontend/.env.example

# Emergent platform internals
.emergent/
.ruff_cache/
test_reports/

# Node
node_modules/
yarn-error.log
firebase-debug.log
.firebase/

# Static collected (generated at build time)
staticfiles/

# Firebase Hosting static output (generated by collectstatic during Cloud Build)
firebase-hosting/static/

# Gemini CLI — commit settings.json and GEMINI.md, but not session history
.gemini/tmp/
.gemini/checkpoints/
.gemini/*.log

# Firebase Admin SDK service account key — NEVER commit
*firebase-adminsdk*.json

# Media uploads (user data - not in repo)
media/

# Data exports (may contain user data)
data_export/
.claude/
.gitconfig
